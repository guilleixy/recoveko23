const parallaxSmooth = document.querySelectorAll('.parallax-smooth');

new simpleParallax(parallaxSmooth,{
    scale:1.2
});

const parallaxUltraSmooth = document.querySelectorAll('.parallax-ultra-smooth');

new simpleParallax(parallaxUltraSmooth,{
    scale:1.05
});